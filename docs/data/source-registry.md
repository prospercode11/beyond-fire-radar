# Source registry

| Provider ID | Source | Coverage | Data type | Current state | Limitations |
| --- | --- | --- | --- | --- | --- |
| `sarasota.official_dispatch` | [Sarasota County 911 Dispatch Reporting interface](https://dispatchreporting.scgov.net/Events?strAgencyID=All) | Sarasota County, FL | Dispatch snapshot | Manual snapshots remain enabled; local development polling is enabled only with the explicit operator basis `explicit_user_permission`, at exactly 15 minutes | Normal HTTPS GET only; no CAPTCHA/access-control/rate-limit bypass; `live_poll` provenance is retained; production/staging require a persisted approved `LegalApproval`; no legal approval or source license is inferred |
| `fixture.sarasota.dispatch` | Repository synthetic fixture modeled on the Sarasota dispatch schema | Synthetic Sarasota County shape | Dispatch snapshot | Enabled for tests and local smoke only | Not an external authority; not accuracy evidence |
| `sarasota.property_appraiser` | Sarasota County Property Appraiser bulk datasets | Sarasota County, FL | Property/tax roll bulk file | Official parcel/sales and detailed files were manually downloaded and imported on 2026-08-02 under `manual_attestation`; automated retrieval remains disabled | Official source approval/terms evidence is not supplied; the manual attestation is not legal approval; versioned mapping/import/provenance/validation are required; synthetic fixtures are not accuracy evidence |
| `fixture.sarasota.property_appraiser` | Repository synthetic fixture modeled on property bulk-file shape | Synthetic Sarasota County shape | Property/tax roll bulk file | Enabled for local tests and manual prototype workflow | Not an external authority; never evidence of property-match accuracy |
| `sarasota.gis` | Sarasota County official GIS/open-data resources | Sarasota County, FL | GIS layers | One Sarasota parcel layer was manually captured for the 2026-08-02 property snapshot; automated GIS retrieval remains disabled | Requires layer-by-layer source/terms verification; the captured geometry is source evidence for the manual snapshot, not a live GIS integration or spatial-accuracy claim |
| `sarasota.permits` | Authorized permit datasets | Sarasota County, FL | Permit records | Not implemented | Manual import first unless authorized integration is confirmed |
| `sarasota.outcomes` | Authorized CAD/fire/public-record/manual outcomes | Sarasota County, FL | Outcome labels | Not implemented | Not assumed real-time; labels require reviewer identity and timestamp |
| `internal.client_roster` | Internal existing-client roster supplied by an authorized user | Internal | Client suppression/reference CSV | Manual/file import enabled for internal workflow only | Not an external authority; rows are retained for provenance and suppression controls; no consumer outreach is implemented |

Every activated provider must populate authority, authorized-use status, enabled state, interval, retrieval/effective times, schema/parser versions, hashes, counts, health, failure state, terms note, and approval/contact note.

## Sarasota manual property snapshot evidence

The 2026-08-02 local/manual import used the official download entries listed on the [Sarasota County Property Appraiser data-download page](https://www.sarasotapropertyappraiser.gov/downloads/download-data/): `SCPA_Parcels_Sales_CSV.zip` and `SCPA_Detailed_Data.zip`. The parcel/sales ZIP SHA-256 is `e7c625aaacd20ab4709da7abfefa54bdff093cc3f59e993d7e0309434b0c1da1`; the detailed ZIP SHA-256 is `6feec5a57a65b119c21918bfb0a888f4a7dd990a0c4fe104e1834f823e7d5a49`.

The normalized import is source version `sarasota.scpa.2026-08-02`, content hash `8b4a3490636c493028df57b231581f9c50880244d20ee923fd73207d0bb9691e`, and property import ID `64e7e421-518b-4ea1-97c5-f3d8701884f0`. It accepted 324,924 parcel rows and recorded 6 missing-address rejections. It retained 250,986 parcel geometries from the official [Sarasota parcel ArcGIS layer](https://services3.arcgis.com/icrWMv7eBkctFu1f/arcgis/rest/services/ParcelHosted/FeatureServer/0), whose captured layer note was `ParcelHosted/0 last edit 2026-08-01T01:05:34Z`. Raw source ZIPs and the normalized CSV remain content-addressed under `data/raw-snapshots/sarasota.property_appraiser`.

This is evidence of a manually supplied prototype snapshot only. It does not establish source terms, legal approval, production authorization, spatial accuracy, or property-match accuracy. Live property polling remains disabled; Sarasota dispatch polling is a separate guarded runtime control.

Incident processing is provenance-gated by acquisition mode: `manual_snapshot` and `synthetic_fixture` remain available for the prototype workflow; `live_poll` is accepted only after the runtime approval decision passes. Local development may use the explicit operator basis `explicit_user_permission`; production/staging require a persisted approved `LegalApproval`. This distinction is retained on retrievals and processing runs and surfaced on incident detail responses.

Phase 5 scoring preserves the same provenance boundary. A score may be computed for research/review from current manual or fixture records, but an operational alert requires only explicitly authorized `manual_snapshot` retrievals with an authorization basis, a resolved property match, no contradiction/negative hard gate, and available fit evidence. Synthetic fixtures and live/unauthorized retrievals cannot satisfy that alert gate; this is an application control, not a legal approval.

Phase 6 adds no external provider. The dashboard reads governed incident/retrieval/property/score APIs, distinguishes live-collected Sarasota records from manual/test records, shows the current guarded 15-minute polling status from `/healthz`, and presents safe empty states when no approved record is available. Its incident-map surface has no live GIS feed and must not be read as geospatial evidence.

Phase 7 adds only the internal client-roster workflow source. Client CSV rows are manually supplied internal reference data, not external-source approval evidence. Internal alert delivery is in-app only; email, SMS, phone, and consumer outreach channels are disabled and unimplemented.

Phase 8 and Phase 9 add no external provider. Human outcome labels and funnel events are internal manual reviewer records with identity, timestamp, rationale, idempotency, and audit provenance. Evaluation manifests and training snapshots retain the dispatch acquisition modes used by their cases; synthetic fixtures remain pipeline evidence only, and the `sarasota.outcomes` external row remains unimplemented. Learning artifacts cannot convert a manual record into source approval, cannot activate live polling, and cannot support an accuracy claim without real approved held-out evidence and administrator approval.

Phase 10 adds no source. Storage backend choice (`local` or S3-compatible), raw-payload retention tombstones/pending purge state, audit-chain integrity, backup manifests, and operations metadata are deployment controls around the same governed records. Dispatch and property records retain their acquisition mode and provenance when payload bytes are purged. The later local runtime activation adds a normal-HTTPS Sarasota scheduler with a database lease and 900-second minimum interval; it does not change the source approval gate, infer legal permission, or enable Boca/Broadcastify/property polling.
