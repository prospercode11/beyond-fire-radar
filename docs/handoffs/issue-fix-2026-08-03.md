# Sarasota issue-fix handoff — 2026-08-03

## Result

The local Beyond Fire Radar workspace is running with the Sarasota issue fixes applied. The API is on `http://127.0.0.1:8000`; the production-style web test server is on `http://127.0.0.1:3021/`.

The current Sarasota ledger contains 84 active canonical incidents. The real incident `11704 ALTAMONTE CT` is one active incident with 39 retained observations after audited duplicate repair. Replaying the latest Sarasota retrieval processed 61 observations with 61 linked, zero new incidents, zero review assignments, and zero contradictions.

The current manual property snapshot `sarasota.scpa.2026-08-02` is visible in the authenticated workbench: 324,924 accepted rows, `manual_snapshot`, `manual_attestation`, and content hash `8b4a3490636c…`. Matching returns parcel `0758080452` at `1.000`. The refreshed score is `55.990`, `Review Only`, `Not eligible`, with `fit_evidence_missing`; this is an intentional hard gate because fit evidence is absent, not a property-match failure.

## Verification

- `./scripts/verify.sh` passed: Ruff format/check, mypy, 67 Python tests, web lint, and Next production build.
- `./.venv/bin/python scripts/dev.py migrate` passed at migration head.
- `./.venv/bin/python scripts/dev.py api-smoke` passed.
- Isolated fresh-DB `scripts/e2e_acceptance.py` passed fixture-only governed workflow checks.
- `scripts/repair_sarasota_duplicate_incidents.py --apply` recorded five exact-key audited legacy merges; the following dry run found no remaining groups.
- Browser verification passed on the running web server for incident selection, current property snapshot visibility, match, score, provenance labels, and the 15-minute Sarasota polling status.
- PostgreSQL/PostGIS and Redis integration execution remains unavailable because Docker/Colima is not running.

## Boundaries

No Boca, Broadcastify, property polling, consumer outreach, learned serving, or source-approval bypass was added. The local Sarasota live scheduler remains enabled only under the existing `explicit_user_permission` runtime basis and the exact 900-second interval. Production/staging remain fail-closed pending persisted `LegalApproval` evidence.

Independent Luna review is recorded in `docs/reviews/issue-fix-2026-08-03-review.md`; no Critical or High finding remains unresolved.
